export type Level = "words" | "sentences";
export type ReadingSpeed = "turtle" | "walk" | "run";

export type Category = {
  id: string;
  name: string;
  file: string;
};

export type ScaleStyles = {
  container: string;
  fontSize: string;
  padding: string;
};
